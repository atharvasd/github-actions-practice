name: pr-check

on:
    pull_request:
        branches: ["main"]
jobs:
    pr_check:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4

            - name: Print PR Check
              run: echo "PR check running for branch':' ${{github.ref_name}}"
